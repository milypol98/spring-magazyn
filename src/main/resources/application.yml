spring:
  datasource:
    url: jdbc:mysql://localhost:3306/magazyn2
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql
